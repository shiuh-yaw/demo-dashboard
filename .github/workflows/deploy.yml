# name: Build and Deploy to Vercel

# on:
#   push:
#     branches: [main, turborepo]
#   workflow_dispatch:

# env:
#   VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     outputs:
#       dashboard: ${{ steps.changes.outputs.dashboard }}
#       checkouts: ${{ steps.changes.outputs.checkouts }}
#       earn: ${{ steps.changes.outputs.earn }}
#       wallet: ${{ steps.changes.outputs.wallet }}
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - uses: dorny/paths-filter@v3
#         id: changes
#         with:
#           filters: |
#             dashboard:
#               - 'apps/dashboard/**'
#               - 'packages/**'
#             checkouts:
#               - 'apps/checkouts/**'
#               - 'packages/**'
#             earn:
#               - 'apps/earn/**'
#               - 'packages/**'
#             wallet:
#               - 'apps/wallet/**'
#               - 'packages/**'

#       - uses: pnpm/action-setup@v4
#         with:
#           version: 9.15.4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: "pnpm"

#       - name: Install dependencies
#         run: pnpm install

#       - name: Build all apps
#         run: pnpm build
#         env:
#           # Server-side placeholders (real values come from Vercel at runtime)
#           COINBASE_API_KEY: placeholder
#           COINBASE_API_SECRET: placeholder
#           LIFI_API_KEY: placeholder
#           # Client-side env vars are baked into the build
#           NEXT_PUBLIC_DYNAMIC_ENVIRONMENT_ID: ${{ secrets.NEXT_PUBLIC_DYNAMIC_ENVIRONMENT_ID }}

#       - name: Upload dashboard build
#         uses: actions/upload-artifact@v4
#         with:
#           name: dashboard-build
#           path: apps/dashboard/.next
#           retention-days: 1

#       - name: Upload checkouts build
#         uses: actions/upload-artifact@v4
#         with:
#           name: checkouts-build
#           path: apps/checkouts/.next
#           retention-days: 1

#       - name: Upload earn build
#         uses: actions/upload-artifact@v4
#         with:
#           name: earn-build
#           path: apps/earn/.next
#           retention-days: 1

#       - name: Upload wallet build
#         uses: actions/upload-artifact@v4
#         with:
#           name: wallet-build
#           path: apps/wallet/.next
#           retention-days: 1

#   deploy-dashboard:
#     needs: build
#     if: github.event_name == 'workflow_dispatch' || needs.build.outputs.dashboard == 'true'
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Download build
#         uses: actions/download-artifact@v4
#         with:
#           name: dashboard-build
#           path: apps/dashboard/.next

#       - name: Install Vercel CLI
#         run: npm i -g vercel

#       - name: Pull Vercel Environment
#         run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
#         working-directory: apps/dashboard
#         env:
#           VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#           VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_DASHBOARD }}

#       - name: Build Vercel Output
#         run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
#         working-directory: apps/dashboard
#         env:
#           VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#           VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_DASHBOARD }}

#       - name: Deploy to Vercel
#         run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
#         working-directory: apps/dashboard
#         env:
#           VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#           VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_DASHBOARD }}

#   # deploy-checkouts:
#   #   needs: build
#   #   if: github.event_name == 'workflow_dispatch' || needs.build.outputs.checkouts == 'true'
#   #   runs-on: ubuntu-latest
#   #   steps:
#   #     - uses: actions/checkout@v4

#   #     - name: Download build
#   #       uses: actions/download-artifact@v4
#   #       with:
#   #         name: checkouts-build
#   #         path: apps/checkouts/.next

#   #     - name: Install Vercel CLI
#   #       run: npm i -g vercel

#   #     - name: Pull Vercel Environment
#   #       run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
#   #       working-directory: apps/checkouts
#   #       env:
#   #         VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#   #         VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_CHECKOUTS }}

#   #     - name: Build Vercel Output
#   #       run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
#   #       working-directory: apps/checkouts
#   #       env:
#   #         VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#   #         VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_CHECKOUTS }}

#   #     - name: Deploy to Vercel
#   #       run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
#   #       working-directory: apps/checkouts
#   #       env:
#   #         VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#   #         VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_CHECKOUTS }}

#   # deploy-earn:
#   #   needs: build
#   #   if: github.event_name == 'workflow_dispatch' || needs.build.outputs.earn == 'true'
#   #   runs-on: ubuntu-latest
#   #   steps:
#   #     - uses: actions/checkout@v4

#   #     - name: Download build
#   #       uses: actions/download-artifact@v4
#   #       with:
#   #         name: earn-build
#   #         path: apps/earn/.next

#   #     - name: Install Vercel CLI
#   #       run: npm i -g vercel

#   #     - name: Pull Vercel Environment
#   #       run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
#   #       working-directory: apps/earn
#   #       env:
#   #         VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#   #         VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_EARN }}

#   #     - name: Build Vercel Output
#   #       run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
#   #       working-directory: apps/earn
#   #       env:
#   #         VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#   #         VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_EARN }}

#   #     - name: Deploy to Vercel
#   #       run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
#   #       working-directory: apps/earn
#   #       env:
#   #         VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#   #         VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_EARN }}

#   # deploy-wallet:
#   #   needs: build
#   #   if: github.event_name == 'workflow_dispatch' || needs.build.outputs.wallet == 'true'
#   #   runs-on: ubuntu-latest
#   #   steps:
#   #     - uses: actions/checkout@v4

#   #     - name: Download build
#   #       uses: actions/download-artifact@v4
#   #       with:
#   #         name: wallet-build
#   #         path: apps/wallet/.next

#   #     - name: Install Vercel CLI
#   #       run: npm i -g vercel

#   #     - name: Pull Vercel Environment
#   #       run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
#   #       working-directory: apps/wallet
#   #       env:
#   #         VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#   #         VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_WALLET }}

#   #     - name: Build Vercel Output
#   #       run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
#   #       working-directory: apps/wallet
#   #       env:
#   #         VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#   #         VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_WALLET }}

#   #     - name: Deploy to Vercel
#   #       run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
#   #       working-directory: apps/wallet
#   #       env:
#   #         VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#   #         VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_WALLET }}
