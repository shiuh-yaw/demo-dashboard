/**
 * Public site header — the canonical implementation now lives in
 * @dynamic-demos/ui (shared with demo scenario pages, e.g. wallet).
 * Defaults ("/" home link, "Demos" chip) are the catalog's own.
 */
export { SiteHeader } from "@dynamic-demos/ui";
