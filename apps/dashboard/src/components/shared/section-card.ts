/** Single source for the operator settings section-card: the faint hairline
 * card wrapping one SettingsSection's fields (one card per section, never
 * card-in-card). */
export const SECTION_CARD = "rounded-xl border border-border bg-card p-4 sm:p-5";
