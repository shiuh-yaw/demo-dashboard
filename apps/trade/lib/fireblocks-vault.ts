/**
 * Trade-specific Fireblocks vault naming.
 * The @dynamic-demos/fireblocks package is generic; this app defines its own prefix.
 */

/** Prefix for user custody vaults in the trade app. */
export const TRADE_VAULT_PREFIX = "Trade Vault - ";
